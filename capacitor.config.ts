import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codesyntax.ionicreactdemo',
  appName: 'ionic-react-leaflet-demo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
